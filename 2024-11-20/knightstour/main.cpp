#include <iostream>
#include <vector>

int main()
{
    int n;
    std::cin >> n;

    std::vector< std::vector< bool > > board;
    board[0].resize(
    bool flag = bt_knights_tour(n, 
    return 0;
}
