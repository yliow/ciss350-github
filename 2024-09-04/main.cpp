#include <iostream>
#include "Matrix.h"

int main()
{
    // Mmxn
    // M4x4

    Matrix m(2, 3);

    // m(0, 0) = 5;
    // m(0, 1) = 0;
    // m(0, 2) = 1;
    // m(1, 0) = 3;
    // m(1, 1) = 7;
    // m(1, 2) = -2;

    // std::cout << m << '\n';

    return 0;
}
